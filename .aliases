if [ "$(command -v exa)" ]; then
	alias ls=exa
	alias l="exa -1"
	alias la="exa -al"
fi

if [[ "$(command -v bat)" ]]; then
	alias cat="bat -pp" #makes plain and removes paging
	alias catp="bat --plain" #makes plain and removes paging
fi

alias slurm="squeue -M ukko2 -l -u $USER"
